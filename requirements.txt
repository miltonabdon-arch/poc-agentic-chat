# Framework real agent_platform_oci - integracao efetiva desta PoC (AD-007).
# Instala libs/agent_framework diretamente do subdiretorio do repositorio
# publico via pip (git+subdirectory) - arrasta suas proprias dependencias
# reais (langgraph>=0.2.60, fastapi>=0.115.0, pydantic>=2.8.0, openai>=1.60.0,
# oci, oracledb, pymongo, redis, mcp, etc; ver
# libs/agent_framework/pyproject.toml no repositorio real).
agent-framework @ git+https://github.com/hoshikawa2/agent_platform_oci.git@main#subdirectory=libs/agent_framework

fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.8.0
python-dotenv>=1.0.1
pyyaml>=6.0.2

# Vector store desta PoC - ver rag_pipeline/vectorizer.py: mantido fora do
# agent_framework.rag.vector_store porque SQLiteVectorStore nao suporta
# upsert por chave externa estavel (sempre gera novo id), incompativel com
# o requisito de reingestao idempotente por chunk_id desta PoC.
# opentelemetry-exporter-otlp-proto-grpc pinado na mesma geração do
# opentelemetry-exporter-otlp-proto-common que o agent_framework arrasta
# (1.44.x); chromadb==0.5.15 puxaria 1.27.0, causando ImportError no boot.
opentelemetry-exporter-otlp-proto-grpc==1.44.0
chromadb==0.5.15
sentence-transformers==3.2.1

httpx>=0.27.0

pytest==8.3.3
pytest-asyncio>=0.23.0
pytest-cov==5.0.0
ruff==0.6.9
